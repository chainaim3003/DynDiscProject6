/**
 * IMPL-V6 — compile anchor (V6.0 scaffold only).
 *
 * PLACEHOLDER: this file exists so `tsc` has at least one input under `src/`
 * and the V6.0 exit criterion ("empty TS compile passes") can be verified.
 * It is replaced by the real MCP-SSE entry (`src/mcp/server-sse.ts`) in V6.2.
 */
export const IMPL_V6 = {
  agent: "jupiterSellerAgent",
  sellerLei: "3358004DXAMRWRUIYJ05",
  buyerLei: "54930012QJWZMYHNJW95",
  iteration: "V6.0",
} as const;
